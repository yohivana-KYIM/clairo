{% extends 'base.html.twig' %}

{% block title %}MailAppli{% endblock %}

{% block body %}
    <h1>MailAppli</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mail_appli.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ mail_appli.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mail_appli_index') }}">back to list</a>

    <a href="{{ path('app_mail_appli_edit', {'id': mail_appli.id}) }}">edit</a>

    {{ include('admin/mail_appli/_delete_form.html.twig') }}
{% endblock %}
