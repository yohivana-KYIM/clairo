<form method="post" action="{{ path('app_mail_appli_delete', {'id': mail_appli.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mail_appli.id) }}">
    <button class="btn">Delete</button>
</form>
