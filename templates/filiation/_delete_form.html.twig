<form method="post" action="{{ path('app_filiation_delete', {'id': filiation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ filiation.id) }}">
    <button class="btn">Delete</button>
</form>
