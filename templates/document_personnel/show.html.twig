{% extends 'base.html.twig' %}

{% block title %}DocumentPersonnel{% endblock %}

{% block body %}
    <h1>DocumentPersonnel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_personnel.id }}</td>
            </tr>
            <tr>
                <th>ArrondissementNaissance</th>
                <td>{{ document_personnel.arrondissementNaissance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_document_personnel_index') }}">back to list</a>

    <a href="{{ path('app_document_personnel_edit', {'id': document_personnel.id}) }}">edit</a>

    {{ include('document_personnel/_delete_form.html.twig') }}
{% endblock %}
