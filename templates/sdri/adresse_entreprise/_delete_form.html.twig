<form method="post" action="{{ path('app_adresse_entreprise_delete', {'id': adresse_entreprise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse_entreprise.id) }}">
    <button class="btn">Delete</button>
</form>
